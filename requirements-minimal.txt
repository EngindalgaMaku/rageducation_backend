# Minimal requirements for testing /models endpoint only
# API server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Configuration management
python-dotenv

# HTTP requests for cloud LLM services
requests>=2.31.0

# Basic dependencies
numpy

# PDF processing - Essential for document processing
PyPDF2
marker-pdf>=0.2.15

# Embeddings - Required for cloud LLM fallback embeddings
sentence-transformers>=2.2.2

# System monitoring - Required by enhanced PDF processor
psutil>=5.9.0

# Vector store for embeddings
faiss-cpu>=1.7.4