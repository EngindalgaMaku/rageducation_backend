# API Gateway Service - Minimal requirements for orchestration only
# Other dependencies moved to individual microservices

# API server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Configuration management
python-dotenv

# HTTP requests for inter-service communication
requests>=2.31.0

# Enhanced logging
colorlog>=6.7.0

# Session management (SQLite is built-in Python)
# No additional requirements needed

# Cloud storage for persistent data (Google Cloud Run)
google-cloud-storage>=2.10.0

# MOVED TO MICROSERVICES:
# - PyPDF2, marker-pdf → pdf_processing_service
# - python-docx, python-pptx → document_processing_service
# - ollama, groq → model_inference_service
# - chromadb, faiss-cpu, numpy → document_processing_service
# - sentence-transformers → model_inference_service
# - psutil → pdf_processing_service (for memory management)
# - pandas, scipy, scikit-learn, plotly, pynvml → removed (not needed in API Gateway)